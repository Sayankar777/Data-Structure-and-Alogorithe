#include <iostream>
using namespace std;
void intersection(int arr[], int brr[], int n)
{
    for (int i = 0; i < n; i++)
    {
        if (arr[i] == brr[i])
    }
}
int main ()
{
    int arr[4] = {1,2,3,4};
    int brr[4] = {2,4,6,8};

    intersection(arr,brr,4);
    return 0;
}